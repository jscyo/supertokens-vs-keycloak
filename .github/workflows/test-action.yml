on: [pull_request]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Install golang
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version: '^1.17'
      - run: go version